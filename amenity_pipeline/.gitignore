node_modules/
dist/
evidence/
*.log
coverage/

# Generated / run artifacts — never commit
output/
output-about/
output-hours/
profiles/
run-batch.cmd
run-about.cmd
run-hours.cmd
run.pid
*-listings.jsonl

# Sweep working files. The run output carries ARIA snapshots and screenshots
# (evidence, sometimes of pages that failed), and the input is regenerated from
# the re-verification queue on every run. The distilled result is committed as
# paradisio_app/data/verified_status.json instead.
sweep.jsonl
# Any run directory, however it was named. Listing them one by one let
# output-probe/ through on 2026-09-05.
output*/
